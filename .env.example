# Copia a .env en la raiz para docker compose (produccion). No lo subas al repositorio.
MYSQL_PASSWORD=
MYSQL_ROOT_PASSWORD=
# Minimo 32 caracteres: openssl rand -base64 48
JWT_SECRET=
CORS_ORIGINS=
TRUST_PROXY=false
ADMIN_EMAIL=
ADMIN_PASSWORD=
API_IMAGE=ghcr.io/axiuz/tupastilla-api:latest
