<?xml version="1.0" encoding="utf-8"?>
<!--
  Un solo layout para los 12 casos de la tarjeta de toma (3 estados x 4 modos).
  El fondo, el circulo del icono y los colores los pone TomaAdapter con EstadoVisual.

  La tarjeta entera es un nodo accesible: el contentDescription va en la raiz y el
  icono se marca como no importante para que TalkBack lea una sola frase.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/tarjeta"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="?attr/tpTouchMin"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="18dp"
    android:focusable="true">

    <TextView
        android:id="@+id/glifo"
        android:layout_width="?attr/tpIconoEstado"
        android:layout_height="?attr/tpIconoEstado"
        android:gravity="center"
        android:textSize="22sp"
        android:textStyle="bold"
        android:includeFontPadding="false"
        android:importantForAccessibility="no" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="16dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/titulo"
            style="@style/T.Tarjeta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/meta"
            style="@style/T.Meta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp" />
    </LinearLayout>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/deshacer"
        style="@style/Widget.TuPastilla.Boton.Texto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:minWidth="0dp"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:text="@string/a1_deshacer"
        android:visibility="gone" />
</LinearLayout>
